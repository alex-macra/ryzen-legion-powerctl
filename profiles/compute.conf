# UNMEASURED STARTING POINT, not a recommendation. These numbers are a hypothesis about
# a Ryzen 9 9955HX3D Legion Pro 7 with the GPU idle: with nothing competing for the
# cooler, the sustained cap should be able to sit well above the crossload share. Run the
# ladder in docs/TUNING.md before trusting any of it, and lower it if the ladder says the
# machine is thermally limited rather than power limited.
DESCRIPTION="CPU-only sustained work with the GPU idle: compiles, renders, batch jobs. Unmeasured; see docs/TUNING.md."
STAPM_W=85
SLOW_W=90
FAST_W=100
TEMP_C=85
POWER_PROFILE=performance
MIN_FREQ_MHZ=stock
MAX_FREQ_MHZ=stock
BOOST=on
EPP=performance
